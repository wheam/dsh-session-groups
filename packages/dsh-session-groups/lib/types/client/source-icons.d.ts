/** Render a stable brand mark when known; preserve the current open/closed folder otherwise. */
export declare function SessionGroupIcon({ source, folded, label }: {
    source?: string;
    folded: boolean;
    label?: string;
}): import("react").JSX.Element;
